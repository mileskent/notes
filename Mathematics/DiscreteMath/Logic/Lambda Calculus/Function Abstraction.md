---
date: 2026-01-22
---
In [[Lambda Calculus]], a *function abstraction* refers to the proccess of defining a [[Mathematics/SetTheory/Function|Function]] by specifying how it [[Transformation|transforms]] an input.

A minimal function abstraction looks like this:
$$
\lambda x.E
$$
* where $\lambda$ indicates this is a function definition
* where $x$ is the input to the function
* where $E$ is the body/definition of the function, where $x$ is transformed somehow

For example, the function that adds one to its input:
$$
\lambda x.x+1
$$

![[Currying]]
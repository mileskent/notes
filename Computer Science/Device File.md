A *device file* is a special type of [[File]] that acts as an interface for reading from and writing to a specific piece of [[Hardware]] (or virtual device). 
* According to [[UNIX]] "Everything is a file" organization
